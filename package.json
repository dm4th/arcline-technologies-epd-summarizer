{
  "name": "arcline-epd-digest",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "probe": "tsx --env-file=.env.local scripts/probe-workspace.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
